# Milestones

## Milestone 1 — Correct the wording  [DONE]

- `openspec_graph/scaffold.py`: `plan_init`'s docstring and generated
  `project.md` content.
- `openspec_graph/cli.py`: module docstring's verb table, `init`
  subparser's `--help` text.
- `README.md`: matching inline comment on the `init` usage example.

- **Gate:** `make pre-pr` green (full suite unmodified and passing
  confirms no behavior change); `planlint validate` clean.
